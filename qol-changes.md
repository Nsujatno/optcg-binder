I wanna modify stuff a little so the ui looks cleaner.

im thinking replacing the left catalog sidebar with an options panel. this will have icons for binder functionality thats not layout or style specific so like
- adding new card
- clearing page
- download as screenshot
- download json, export json

we will switch adding new cards as a modal now. so clicking add card button on new left sidebar will open up a catalog model similar to what we have now, search feature, sets in order.

we will now allow functionality for adding cards directly to card slots on the page. so when you click there will be two icons now a + button for adding a card, the current share button that lets u upload the michi method images. these icons should have hover states that tell the user so like for the + it should be "add a card here" and for the share button it should be "add an image"

i would also later want to make the binder size smaller so a 3x3 fits a standard size laptop at 100% zoom. currently for it to look good i need to zoom out by like 80%.